name: brew-tap

on:
  # release 由 GITHUB_TOKEN 创建，不会触发 release 事件（防递归），故用链式触发
  workflow_run:
    workflows: [release]
    types: [completed]
  workflow_dispatch:
    inputs:
      version:
        description: "要发布的版本（如 1.7.0 或 2.0.0-alpha.1；预发布使用独立通道）"
        required: true

concurrency:
  group: brew-tap-publish
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  update-tap:
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success') }}
    steps:
      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y ruby

      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v7
        with:
          version: '0.6.17'
          python-version: '3.12'
          enable-cache: true
      - name: Sync Python tools
        run: uv sync --locked

      - name: Test release resolution
        run: uv run python -m unittest discover -s packaging -p 'test_*.py'

      - name: Resolve published release
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
          REQUESTED_VERSION: ${{ inputs.version }}
          RELEASE_BRANCH: ${{ github.event.workflow_run.head_branch }}
          RELEASE_SHA: ${{ github.event.workflow_run.head_sha }}
          ALLOW_PRERELEASE: 'true'
        run: uv run python packaging/release-version.py

      - name: Set up Homebrew for runtime validation
        if: steps.release.outputs.publish == 'true'
        uses: Homebrew/actions/setup-homebrew@1f8e202ffddf94def7f42f6fa3a482e821489f9c # 2026.07.10.1

      - name: Setup GitHub SSH
        if: steps.release.outputs.publish == 'true'
        env:
          GH_KEY: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
        run: |
          install -dm700 ~/.ssh
          printf '%s\n' "$GH_KEY" > ~/.ssh/gh_key
          chmod 600 ~/.ssh/gh_key
          printf 'Host github.com\n  User git\n  IdentityFile ~/.ssh/gh_key\n  StrictHostKeyChecking accept-new\n' > ~/.ssh/config

      - name: Update CLI formula and GUI cask
        if: steps.release.outputs.publish == 'true'
        env:
          VERSION: ${{ steps.release.outputs.version }}
        run: |
          git config --global user.name "mizorewww (ci)"
          git config --global user.email "mizorewww@users.noreply.github.com"
          tap="$(brew --repository)/Library/Taps/mizorewww/homebrew-tap"
          mkdir -p "$(dirname "$tap")"
          git clone -q git@github.com:mizorewww/homebrew-tap.git "$tap"
          uv run python packaging/homebrew/update.py "$tap" --version "$VERSION"
          HOMEBREW_NO_AUTO_UPDATE=1 brew readall --aliases mizorewww/tap
          cd "$tap"
          if git diff --quiet && [ -z "$(git ls-files --others --exclude-standard)" ]; then
            echo "tap already publishes ${VERSION}"
            exit 0
          fi
          git add --all -- Formula Casks
          if [ -d Aliases ]; then git add --all -- Aliases; fi
          git commit -qm "course2md ${VERSION}"
          git push -q origin main
          echo "homebrew-tap updated to ${VERSION}"
